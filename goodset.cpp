#include <bits/stdc++.h>
using namespace std;
int main()
{
    int test;
    cin>>test;
    for(int i=0;i<test;i++)
    {
        int p;
        cin>>p;
        vector<int> v;
        v.push_back(1);
        while(p!=v.size())
        {
            v.push_back(2);
            if(p==2)
                break;

        }
    }
}
